{
  "name": "cove",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@supabase/supabase-js": "^2.98.0",
    "posthog-js": "^1.357.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "CI=false react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "construction:on": "cp public/repairs.html public/index.html && git add public/index.html && git commit -m 'cove: construction page up' && git push origin main",
    "construction:off": "cp public/app-index.html public/index.html && git add public/index.html && git commit -m 'cove: app restored' && git push origin main"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
